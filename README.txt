ott2 folder contains the Angukar frontend
run "ng serve --port 44375" inside angularapp folder

ott3 folder contains the C# backend using Entity Framework(ORM), requires Visual Studio 22 to run.
before running back end run
"Add-Migration m1"
"Update-Database"
Run the backend in VS 22. Swagger will open and expose the api's.